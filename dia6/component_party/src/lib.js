export const sumarv = (num1, num2) => {
    return num1+num2
}

export const getRelativeTime = (date) => {

    // Obtener la fecha que le mando (date)
    // Obtener la fecha de AHORA!!

    

    // Restar las 2 fechas

    // Contar cuanto tiempo paso entre una y otra... (segundos, minutos, dias, años, etc...)

}